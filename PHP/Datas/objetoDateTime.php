<?php

    // Com o objeto dateTime() permite tratar a data como um objeto;
    // Podemos passar um parâmetro que será a data criada, se não passar nada, será criada a data atual;
    // Para exibir as informações é necessária o print_r()

    $date = new DateTime();
    print_r($date);