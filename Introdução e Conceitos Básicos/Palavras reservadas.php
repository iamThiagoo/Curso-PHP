<?php
    // Palavras reservadas que não podem aparecer em funções, etc.
    // echo, insteadof, else, interface, namespace, pow, _DIR_, _FILE_, endif, print, private, protected, and, required, public, as
    // break, case,for, finally, switch, throw, etc;

    function echo(){ // COM ERRO
        return true;
    }

    function Thiago(){ // SEM ERRO
        return true;
    }
?>